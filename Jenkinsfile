pipeline{
		agent {label 'master'}
				stages{
				       stage('one'){
								steps{
									echo 'welcome to jenkins baba'
								}
					   }
					   stage('build'){
								steps{
								echo 'building info'
								}
					   }
				}
}
