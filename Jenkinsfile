pipeline{
		agent {label 'master'}

	figlet{ Welcomw to Jenkins }
				stages{
				       stage('one'){
								steps{
									echo 'welcome to jenkins baba'
								}
					   }
					   stage('build'){
								steps{
								echo 'building info' 
								}
					   }
				}
}
