pipeline{
		agent {label 'master'}

		figlet('WElcome to MAster')
				stages{
				       stage('one'){
								steps{
									echo 'welcome to jenkins baba'
								}
					   }
					   stage('build'){
								steps{
								echo 'building info' 
								}
					   }
				}
}
