pipeline{
		agent {label 'master'}
	
				stages{
				       stage('one'){
								steps{
									figlet "Welcome to Jenkins"
									echo 'welcome to jenkins baba'
								}
					   }
					   stage('build'){
								steps{
								echo 'building info' 
								}
					   }
				}
}
